import Title from "../components/Title";

const Education = () => {
    return (
        <div>
            <Title {...{
                text: "Education",
                subText: "My educational background"
            }} />
            <ul>
                <il>
                </il>
            </ul>
        </div>
    );
};

export default Education;
